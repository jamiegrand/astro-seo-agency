# ============================================
# ASTRO SEO AGENCY - Environment Configuration
# ============================================
# Copy this file to .env and fill in your values
# All variables are OPTIONAL - the plugin works without them
# ============================================


# ============================================
# GOOGLE ANALYTICS (Optional)
# ============================================
# Enables: Traffic data in /start, /content-roi, /impact
# 
# How to find your Property ID:
#   1. Go to https://analytics.google.com
#   2. Admin (gear icon) → Property Settings
#   3. Copy the "Property ID" (numbers only)
#
# Example: GA_PROPERTY_ID=123456789

GA_PROPERTY_ID=


# ============================================
# GOOGLE SEARCH CONSOLE (Optional)
# ============================================
# Enables: /seo-wins, ranking data in /start, /impact
#
# GSC_SITE_URL: Your site exactly as shown in Search Console
#   - Include https:// and trailing slash
#   - Example: https://example.com/
#
# GOOGLE_APPLICATION_CREDENTIALS: Path to service account JSON
#   - See setup instructions below

GSC_SITE_URL=
GOOGLE_APPLICATION_CREDENTIALS=./credentials/gsc-service-account.json

# ----- GSC Service Account Setup -----
# 
# 1. Go to https://console.cloud.google.com
# 2. Create a new project (or select existing)
# 3. Enable the "Search Console API":
#    - APIs & Services → Library → Search "Search Console API" → Enable
# 4. Create a Service Account:
#    - APIs & Services → Credentials → Create Credentials → Service Account
#    - Name it something like "astro-seo-agency"
#    - Skip optional permissions, click Done
# 5. Create a key:
#    - Click on the service account → Keys → Add Key → Create new key → JSON
#    - Save the downloaded file to ./credentials/gsc-service-account.json
# 6. Add to Search Console:
#    - Copy the service account email (ends in @...iam.gserviceaccount.com)
#    - Go to https://search.google.com/search-console
#    - Settings → Users and permissions → Add user
#    - Paste the email, set permission to "Full"
#
# Done! The plugin can now access your Search Console data.


# ============================================
# GITHUB (Optional)
# ============================================
# Enables: GitHub Issues integration for /fix-next
#
# GITHUB_TOKEN: Personal access token
#   1. GitHub → Settings → Developer settings → Personal access tokens
#   2. Generate new token (classic)
#   3. Select scope: "repo" (Full control of private repositories)
#   4. Copy the token (starts with ghp_)
#
# GITHUB_REPO: Your repository in format username/repo-name

GITHUB_TOKEN=
GITHUB_REPO=


# ============================================
# BRAVE SEARCH (Optional)
# ============================================
# Enables: Competitor research in /seo-wins
#
# Get your API key at: https://brave.com/search/api/

BRAVE_API_KEY=


# ============================================
# NOTES
# ============================================
# 
# Security:
#   - This file should be in .gitignore (added automatically)
#   - Never commit credentials to version control
#   - The ./credentials/ directory is also gitignored
#
# Without Analytics:
#   - /start shows issue priorities (without traffic data)
#   - /fix-next prioritizes by severity instead of traffic impact
#   - /seo-wins does content-based analysis instead of GSC data
#   - /audit, /feature, /pause, /resume work fully
#
# Testing your setup:
#   - Run /start in Claude Code
#   - If analytics are configured, you'll see traffic data
#   - If not, you'll see a message about configuring them
